from setuptools import setup, find_packages

setup(
    name = 'slab',
    version = '0.1',
    package = find_packages(),
    description = 'package for chips',
    author = 'David Schuster',
    url = 'None',
    license = 'None',
)
